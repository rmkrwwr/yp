<h1>удаление препода</h1>

<p>точно удалить "{{ teacher.first_name }} {{ teacher.last_name }}"?</p>

<form method="post">
    {% csrf_token %}
    <button type="submit">да, удалить</button>
    <a href="/schedule/teachers/">нет, отмена</a>
</form>